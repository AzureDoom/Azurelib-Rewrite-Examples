{
  "schemaVersion": 1,
  "id": "azexamples",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${mod_url}",
    "issues": "${mod_issues}",
    "sources": "${mod_sources}"
  },
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "mod.azure.azexamples.FabricLibMod"
    ],
    "client": [
      "mod.azure.azexamples.FabricLibClientMod"
    ]
  },
  "mixins": [
    "azexamples.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "java": ">=${java_version}",
    "azurelib": "~${azurelib_version}",
    "minecraft": "~${minecraft_version}"
  }
}
